/**
* Provides the base Widget {{capitalize}}
*
* @module {{name}}
* @submodule posApp
*/
define([], function () {
    'use strict';
    var {{capitalize}}Services = {
        /**
        * Provides Service to {{capitalize}}
        *
        * @class {{capitalize}}Services
        * @constructor
        * @module {{name}}
        */
        {{name}}Service: function ($trHttp) {
            return function (path, callback) {
                $trHttp.get(path).success(function (data) {
                    if (callback) callback(data);
                });
            }
        }
    };
    return {{capitalize}}Services;
});