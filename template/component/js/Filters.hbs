/**
* Provides the base Widget {{capitalize}}
*
* @module {{name}}
* @submodule {{pref}}App
*/
define([], function () {
    'use strict';

    var {{capitalize}}Filters = {
        /**
        * Provides Filter to {{capitalize}}
        *
        * @class {{capitalize}}Filters
        * @constructor
        * @module {{name}}
        */
        {{name}}Filter: function () {
            return function(i){
                //return
            }
        }
    };
    return {{capitalize}}Filters;
});
