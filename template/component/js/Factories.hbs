/**
* Provides the base Widget {{capitalize}}
*
* @module {{name}}
* @submodule {{pref}}App
*/
define(['angular'], function (angular) {
    'use strict';

    var {{capitalize}}Factories = {
        /**
        * Provides Factory to {{capitalize}}
        *
        * @class {{capitalize}}Factories
        * @constructor
        * @module {{name}}
        */
        {{name}}Factory: function ({{name}}Service) {
            var factory={};

            return factory;
        }
    };
    return {{capitalize}}Factories;
});